[package]
name = "api-canil"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.7", features = ["macros", "multipart"] }
tokio = { version = "1.0", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sqlx = { version = "0.7", features = ["sqlite", "runtime-tokio-native-tls"] }
dotenvy = "0.15"
bcrypt = "0.15"
jsonwebtoken = { version = "10.4.0", features = ["rust_crypto"] }
chrono = { version = "0.4", features = ["serde"] }
tower-http = { version = "0.5", features = ["cors", "fs"] }
uuid = { version = "1.8", features = ["v4"] }
image = "0.25"
[profile.release]
opt-level = 3  # Otimizar para desempenho
lto = true       # Link Time Optimization (remove código não utilizado)
codegen-units = 1 # Reduz o paralelismo para maximizar a otimização
panic = "abort"  # Remove o unwinding de panic, diminuindo o binário
strip = true     # Remove símbolos de debug
